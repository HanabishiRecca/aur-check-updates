[package]
name = "aur-check-updates"
version = "1.0.3"
edition = "2021"
rust-version = "1.70"

[profile.release]
panic = "abort"

[dependencies]
alpm = "2.2"
curl = "0.4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
