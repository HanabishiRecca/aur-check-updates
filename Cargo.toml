[package]
name = "aur-checkupdates"
version = "0.1.0"
edition = "2021"

[profile.release]
panic = "abort"

[dependencies]
alpm = "2.2"
configparser = "3.0"
curl = "0.4"
libc = "0.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
