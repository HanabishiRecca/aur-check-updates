[package]
name = "aur-check-updates"
version = "1.0.2"
edition = "2021"

[profile.release]
panic = "abort"

[dependencies]
alpm = "2.2"
curl = "0.4"
libc = "0.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
